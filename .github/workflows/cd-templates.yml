# Deploy templates on playground as soon as changes are commited in conf/../templates/* files
name: Continuous Deployment templates

on:
  push:
    branches:
      - develop
      - master
    paths:
      - 'conf/k8s/overlays/*/templates/*'

env:
  NAMESPACE: playground
  GKE_PROJECT: gke_substrateplayground-252112
  GKE_ZONE: us-central1-a

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Set env to staging
        if: endsWith(github.ref, '/develop')
        id: env
        run: |
          echo "::set-output name=environment::staging"
          echo "::set-output name=cluster::susbtrate-playground-staging"

      - name: Set env to production
        if: endsWith(github.ref, '/master')
        run: |
          echo "::set-output name=environment::production"
          echo "::set-output name=cluster::substrate-playground"

      - uses: actions/checkout@v2

      - uses: GoogleCloudPlatform/github-actions/setup-gcloud@master
        with:
          service_account_key: ${{ secrets.GCLOUD_KEY }}
          export_default_credentials: true
          project_id: ${{ env.GKE_PROJECT }}
      - run: gcloud --quiet auth configure-docker
      - run: gcloud container clusters get-credentials "${{ steps.env.outputs.cluster }}" --zone "$GKE_ZONE"

      - name: Deploy templates
        run: kubectl create configmap templates --namespace=${NAMESPACE} --from-file=conf/k8s/overlays/${{ steps.env.outputs.environment }}/templates/ --dry-run=client -o yaml | kubectl apply -f -

# TODO test image, rollback commit if fails
# Do not rollback the rollback itself
# GITHUB_REF
# https://engineering.bitnami.com/articles/rolling-updates-for-configmap-objects.html